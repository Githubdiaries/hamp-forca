[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: Pure unit tests (no database or TensorFlow required)
    integration: Tests requiring PostgreSQL
    ml: Tests requiring TensorFlow and PostgreSQL
